[tool.poetry]
name = "tax"
version = "0.1.0"
description = "Django Tax App for Saas"
authors = ["Matt Strayer <git@mattstrayer.com>"]

[tool.poetry.dependencies]
python = "^3.8"
django = "^3.0.5"

[tool.poetry.dev-dependencies]
black = "^19.10b0"
flake8 = "^3.7.9"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
